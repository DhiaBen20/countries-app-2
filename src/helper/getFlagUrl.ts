export function getFlagUrl(code: string) {
    return `https://flagicons.lipis.dev/flags/4x3/${code.toLowerCase()}.svg`
  return `https://flagcdn.com/56x42/${code.toLowerCase()}.png`;
  return `https://flagsapi.com/${code}/flat/64.png`;
}
